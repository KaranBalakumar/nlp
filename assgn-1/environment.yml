name: nlp
channels:
  - conda-forge
  - defaults

dependencies:
  - python=3.11
  - numpy
  - scikit-learn
  - pip
  - psutil
  - tqdm
  - pip:
    - --find-links https://storage.googleapis.com/jax-releases/jax_cuda_releases.html
    - "jax[cuda12_pip]==0.4.20"
    - "jaxlib[cuda12_pip]==0.4.20"
    - regex
